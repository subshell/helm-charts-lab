apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dependency-db.fullname" . }}
  labels:
    {{- include "dependency-db-api.labels" . | nindent 4 }}
data:
  application.yml: |- {{  toYaml (required "A valid application.yaml config is required (nested in the value .Values.api.config)!" .Values.api.config ) | nindent 4 }}
